import dotenv from "dotenv";

dotenv.config();

export const _config = {
    SMTP_HOST: "smtp.gmail.com",
    SMTP_PORT: 587,
    SMTP_SERVICE: "gmail",
    SMTP_USER: "ashishrahulraj2005@gmail.com",
    SMTP_PASS: "zwpk oetq ufal isnm", // Use App Password if 2FA is enabled
};
